# ===== 私有 / 大文件（不进仓库）=====
# 受版权保护的原始文献扫描件（约 158MB）
*.pdf

# OCR 管线产物：全书 OCR 文本 + 中文语言模型 + 逐页中间文件（共约 921MB，含私有内容）
.ocr/

# WorkBuddy 内部项目记忆 / 自动化配置（非交付物）
.workbuddy/

# ===== Python 缓存 =====
__pycache__/
*.py[cod]
*.egg-info/

# ===== 临时 / 备份 / 可再生的中间产物 =====
*.bak
*.bak_*
*.bak-*
index.html.bak_*
CAUSAL_INFERRED_reconstructed.json

# 页面历史备份目录
html-backups/

# 根目录扫描中间产物（正式报告在 scripts/clean_ocr_report.md）
/scan_ocr_report.md

# Python 虚拟环境
.venv/
venv/

# ===== 系统 / 编辑器 =====
.DS_Store
Thumbs.db
*.swp
._*

# OCR 清洗工作目录（临时）
.ocr_work/

# OCR 清洗索引中间产物（.tmp_*.json）
.tmp_*.json

# OCR 进度 / 质检脚本生成的报告（可重新生成）
scripts/ocr_status.md
scripts/qc_ocr_shortlist.md
scripts/rel_candidates.md

# 构建临时日志（工作台「?? 构建」产生的 _build*.log）
_build*.log

# 工具备份目录（合并/改动前的整目录快照，可删可留，不进仓库）
tools/_backup_*/

# events/causality 统一备份目录（各工具写入前自动快照）
backups/

# 设计稿 / 原型（页面功能已落地后不再进仓库）
mockup_*.html
