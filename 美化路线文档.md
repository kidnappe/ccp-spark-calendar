# 星火日历 · 界面美化路线文档

> 项目：星火日历（单文件 HTML 应用，`index.html`）
> 设计语言：宣纸报刊档案
> 记录范围：本轮交互打磨中的所有视觉 / 动效美化工作
> 校验状态：全部通过 `node --check`，无残留旧引用

---

## 一、设计语言基底

### 1.1 配色 Token（亮 / 暗两套，经 `[data-theme]` 切换）
- 宣纸底：亮 `#f6f0e3` / `#f0e9d8`；暗 `#1a1a17`
- 米纸面：亮 `#fdfaf2` / `#faf6ea`；暗 `#232019` / `#2b2822`
- 朱砂（主强调 / 印章）：亮 `#8b1a1a` / `#b22222`；暗字标 `#e57373`
- 描边：亮 `#c8bd9f` / `#d6c9a8`；暗 `#3a362c` / `#4a4334`
- 墨字：亮 `#5a4a33`；暗 `#ddd8cd`
- 蓝（其他力量）：`#1a5276`；金（分类强调）：`#b7791f`

### 1.2 字体
全局宋体（`SimSun` / `Microsoft YaHei` 兜底），呼应报刊档案气质。

---

## 二、美化路线总览（按落地阶段）

### 阶段 0 · 布局修正：顶栏与悬浮按钮
- 顶栏（双线报头）回归正常文档流，不固定；
- 三个悬浮按钮（logo 字标 / 本年事件 / 日夜切换）改为 `position:absolute`，**随页面滚动**，钉在背景板上而非视口；
- 修正了早期误将顶栏设为 `fixed` 的偏差。

### 阶段 1 · 时间轴「每日一史」入口
- 时间轴模式工具栏原「共 N 个事件」计数，替换为「每日一史」按钮，复用日历页 `.cal-today` 样式，点击行为绑定 `openTodayInHistory`。

### 阶段 2 · B 组动效（报刊叙事动效）
遵循 impeccable 动效规范，落地两个叙事切换：
- **B1 卷轴横向展开**：进入时间轴模式时，`.tl-scroll` 以 `clip-path: inset(0 50% 0 50%) → inset(0)` 从中央向两侧揭开；左右两端 `.tl-rod` 木轴随纸面外移（`rodOutL/R`），保留轻微回弹 `cubic-bezier(0.34,1.56,0.64,1)`；退出时 `roll-up` 反向卷起（离场用 ease-in `cubic-bezier(0.7,0,0.84,0)`）。
- **B2 日历毛边撕口**：仅**切换年份**（及月份）时生效。旧内容以顶部 16 齿毛边 `clip-path` 上掀离场（`calTearOut`：顶为轴 `translateY(-118%) rotate(-3deg)` ease-in），新内容从顶部毛边落入抚平（`calTearIn`，轻微回弹）。由 `animateCalendarSwap(rebuild)` 统一驱动，`setYearIndex` / `selectMonth` 调用；模式切换不触发此动画。

### 阶段 3 · 图例固定 + 月份动画 + 右侧抽屉
- **图例固定**：把 `■ 中共 / ■ 其他力量 / ■ 国家大事` 从 `#calendarView` 内移出，成为同级 `.cal-legend-row`，与双线报头、按钮控件一同不参与动画位移；
- **月份切换动画**：`selectMonth` 复用 `animateCalendarSwap`，月份切换也走毛边撕口；
- **右侧抽屉**：新增 `.menu-drawer`（页面右侧边缘滑入 `translateX(100%)→0`）+ `.menu-drawer-mask` 遮罩，替代原「更多」下拉；默认隐藏，点击右上 ☰ 打开，内含原更多菜单全部按钮（会议 / 关于 / 收藏 / 统计 / 标签等）。

### 阶段 4 · 菜单按钮布局修正
- 菜单按钮曾与侧栏按钮重叠 → 重排为同行三钮：`本年事件(right:120) → 日夜切换(right:64) → 菜单(right:14, z:60 置顶)`，在各断点（768 / 700 / 480）复查无遮挡。

### 阶段 5 · 五类「报刊按钮体系」（本轮收口）
收敛全站按钮为 5 类，统一微交互：
1. **① 印章主钮 / ② 铅字次钮**：工具栏、模式、时间轴、因果图、分类、月份、菜单项、本年事件等纸面按钮，统一 `transition: transform .32s var(--ease-quint)`，悬浮上抬 2px + 暖投影，按下 `scale(.96)`，键盘 `:focus-visible` 朱砂环。
2. **③ 报刊幽灵钮**：`ghost-btn` 改为真·幽灵（透底 + 朱砂描边 + 朱砂字）。
3. **④ 报头文字钮**：年份箭头 / 年份标签 / 每日一史 下划线 `scaleX` 自左画出（不位移），年份箭头按下收缩。
4. **⑤ 圆钮**：主题钮 / 菜单钮 / 弹窗 × / 抽屉 × / 侧栏 ×，悬浮 `scale(1.06)` + 投影，按下 `scale(.92)`。
5. **模式切换朱砂滑块**：新增 `.mode-pill`，切「日历 / 时间轴」时朱砂印章在两段间滑行（`positionModePill` 实时测位）；首次加载 `requestAnimationFrame` 瞬时不带动画，避免从 0 生长。

---

## 三、动效技术规范（impeccable 约束）

| 项 | 规范 |
|---|---|
| 可动属性 | 仅 `transform` / `opacity` / 色彩；不触发布局位移 |
| 入场缓动 | `ease-out-quint` `cubic-bezier(0.22,1,0.36,1)` |
| 离场缓动 | `ease-in` `cubic-bezier(0.7,0,0.84,0)` |
| 回弹 | 默认禁用；**用户特例**：卷轴（B1）、日历撕口落入（B2）保留 `cubic-bezier(0.34,1.56,0.64,1)` 轻微过冲 |
| 无障碍 | 全部动效包在 `@media (prefers-reduced-motion: reduce)` 内 `animation:none !important` |
| 打印 | `@media print` 隐藏抽屉 / 侧栏 / 浮钮 |

---

## 四、落地与校验

- 文件：`E:\code\ccp-spark-calendar\index.html`（单文件，无构建）。
- 数据由 `scripts/build_data.py` 重写，本轮未改数据。
- 校验：两段内联 `<script>` 均通过 `node --check`；grep 确认无 `more-menu` / `yearAnimating` 等残留引用。
- 已知空位：`ghost-btn` 体系样式已备，但当前 HTML 暂无实例使用（留作后续按钮接入）。

---

## 五、暂缓 / 未落地（详细规格）

> 以下三项已确认要做，但尚未落地。本节给出可直接实现的规格，落地前需先按「前置核对」确认数据字段与现有函数钩子。

### 5.1 B3 · 每日一史「扭蛋机」胶囊

**目标**：把「每日一史」入口（`日历页 .cal-today` / `时间轴 todayBtnTL` → `openTodayInHistory()`）从直接弹卡，改为「报刊印章胶囊落入 → 两瓣裂开 → 露出今日史卡片」的轻叙事动效，呼应 B 组报刊动效语言。

**触发场景**
- 点击日历页「每日一史」或时间轴「每日一史」按钮时播放；仅胶囊出现过程有动效，卡片内容本身静态。

**视觉与动效（impeccable 约束）**
- 胶囊：朱砂描边 + 米纸面 + 顶部小金钮的竖向胶囊（`.gacha-capsule`，约 220×320），居中浮于遮罩。
- 时序：
  1. **落入**：`translateY(-40px)→0`、`opacity 0→1`，`ease-out-quint` `.42s`，**落定无回弹**（与 B2 区别，用户原话「落定无回弹」）。
  2. **停顿** ~200ms。
  3. **裂开**：左瓣 `.gacha-half-l` `translateX(-62%) rotate(-8deg)`、右瓣 `.gacha-half-r` `translateX(62%) rotate(8deg)`，`ease-out-quint` `.4s`；卡片 `.gacha-card` 同步 `scale(.96)→1` + `opacity 0→1`。
  4. **关闭**：点遮罩 / × → 胶囊两瓣反向合拢或直接淡出（`opacity→0`），`.3s`。
- **只动 `transform`/`opacity`**；全部包进 `@media (prefers-reduced-motion: reduce)` → 直接显示卡片、跳过胶囊；`@media print` 隐藏胶囊与遮罩。

**落地位置**
- DOM：在现有详情/弹窗容器外新增 `<div class="gacha-capsule">…</div>` + 遮罩；HTML/CSS 沿用 `.modal-x`、`.modal-mask` 体系。
- JS：在 `openTodayInHistory()` 内，先播胶囊再注入卡片内容；`reduced` 标志走现有 `window.matchMedia('(prefers-reduced-motion: reduce)')` 分支。

**验收**：胶囊裂开无布局抖动；键盘 `Esc` / 点遮罩可关；首屏无横向位移；reduced-motion 下等价可用。

**前置核对**：确认 `openTodayInHistory` 当前渲染目标（是弹窗还是时间轴定位），避免与 B1 卷轴动画叠加冲突。

---

### 5.2 D 组 · 移动端适配

**目标**：在 ≤768 / 700 / 480 三档断点（部分已存在，需统一收口）解决三类问题：横滚、顶部遮挡、横屏。

**① 禁横滚**
- `html, body { overflow-x: hidden; }` 兜底；时间轴 `.tl-scroll` 在窄屏由横向滚动改为**纵向堆叠**（每格整宽、向下排），或保留横向但锁定在 `.tl-scroll-wrap` 内部（`touch-action: pan-x` + 容器 `max-width:100%`），绝不外溢到 `body`。
- 日历网格：小屏可分区横向滚动，但容器自身裁切，不撑出视口。

**② 顶部遮挡**
- 三浮钮（本年事件 `right:120` / 日夜 `right:64` / 菜单 `right:14`）在 <768 重排为更紧凑尺寸，确保不遮日历首行、不遮时间轴工具栏；报头双线报头字号下调。
- 右侧抽屉 `.menu-drawer` 在移动端改为**全宽**（`width:100%`），不再右侧窄抽屉；弹窗/详情同理由右侧栏改全屏或底部 sheet。

**③ 横屏（orientation: landscape）**
- `@media (orientation:landscape) and (max-height: 500px)`：时间轴高度自适应、工具栏换行；浮钮缩小并贴边，避免遮挡内容。

**落地位置**
- CSS：在现有 `@media (max-width:768px)/(700px)/(480px)` 块内补全；新增 `orientation` 媒体查询。
- 与 B1/B2 动画：移动端动画时长可略缩短（`.unroll`/`.tear` 由 `.8s/.72s` 降至 `.5s`），减少等待。

**验收**：DevTools 设备模拟三档 + 横屏各跑一遍——无横向滚动条、所有按钮可点不遮挡、时间轴/日历可读。

**前置核对**：盘点现有 768/700/480 断点里已写的规则，避免重复或冲突覆盖。

---

### 5.3 E 组 · 史料署名

**目标**：事件详情页「史料详情」面板（F1 已落地，6 子块 `bg/significance/quotes/figures/srcCite/furtherReading`）中，把**引用与来源显式署名**，提升史料可信度与报刊考据感。

**范围**
- `quotes`：每条补充 `by`（作者）与 `src`（出处，如《xxx 文献》《xxx 报刊》）；渲染为刊内引文样式——左侧朱砂竖线 + 斜体，署名右对齐 `—— 作者《出处》`。
- `srcCite`：渲染为「史料来源」条目块（小字墨色 + 上分隔线）。
- `furtherReading`：渲染为「延伸阅读」可点链接列表。

**视觉（报刊档案语言）**
- 引文：左 `3px` 朱砂竖线 + 宋体斜体；署名 `text-align:right`、墨字 `#5a4a33`、字号小一号。
- 来源/延伸：面板底部脚注区，与正文用 `border-top:1px dashed` 分隔。

**落地位置**
- 数据：`build_data.py` 已透传 6 子块；需确认 `quotes` 数组元素是否含 `by/src` 字段，缺失则在工具链（`gen_detail.py`/`detail_rich_gui.html`）补采集。
- 渲染：详情面板渲染函数（F1 的 `renderDetail` / `gen_detail`）中按上述样式输出；`by` 缺失时回退标注「佚名 / 未署」，不报错、不空白。

**验收**：每条引文均有署名；来源/延伸可读可点；缺字段时优雅降级不破版。

**前置核对**：抽样 3–5 条事件的 `quotes` 数据，确认字段命名（`by`/`src` 或现有别名），避免渲染时取错键。

---

### 5.4 落地优先级建议

| 项 | 依赖 | 建议顺序 |
|---|---|---|
| E 组 史料署名 | 数据字段核对 + 渲染函数小改 | 先（纯展示，风险低） |
| D 组 移动端 | 现有断点盘点 | 次（影响面大，需多档测试） |
| B3 扭蛋机 | `openTodayInHistory` 钩子核对 | 后（动效叠加，需防与 B1 冲突） |
